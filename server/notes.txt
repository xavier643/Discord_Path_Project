fly apps create discord-path-api     # or use your chosen app name once

fly secrets set \
  MONGO_URL="mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority" \
  MONGO_DB="discord_path" \
  DISCORD_CLIENT_ID="<id>" \
  DISCORD_CLIENT_SECRET="<secret>" \
  DISCORD_REDIRECT_URI="https://<your-app>.fly.dev/auth/discord/callback" \
  DISCORD_BOT_TOKEN="<bot-token>" \
  SESSION_SECRET="<random-long-string>" \
  ALLOWED_ORIGIN="https://<your-frontend-domain>" \
  POST_LOGIN_REDIRECT="/"

!!! local running 
python -m venv venv && venv\Scripts\activate   # Windows
pip install -r requirements.txt
# Ensure your .env values are exported or use something like "set VAR=..."
set FLASK_APP=server.app
python -m server.app
# open http://localhost:5000/auth/discord/login
